# -*- coding: utf-8 -*-
"""
常量定义文件
将项目中的魔法数字和硬编码值集中管理
"""

# ============================================================================
# API 限流配置 (API Rate Limiting)
# ============================================================================
API_REQUEST_DELAY_SECONDS = 0.5  # 每次 API 请求之间的延迟
BATCH_PROCESSING_DELAY_SECONDS = 3  # 批处理之间的延迟，避免触发墨墨限制

# 重试配置
MAX_RETRIES = 3
RETRY_WAIT_SECONDS = [10, 25, 60]  # 每次重试的等待时间

# ============================================================================
# 批处理配置 (Batch Processing)
# ============================================================================
DEFAULT_BATCH_SIZE = 20  # 推荐批次大小（平衡 API 成本与 token 限制）
MAX_SAFE_BATCH_SIZE = 25  # 超过此值，AI 输出格式崩溃概率增加
WORDS_PER_BATCH_LIMIT = 100  # 单次处理的最大单词数

# ============================================================================
# 数据库配置 (Database)
# ============================================================================
DB_CONNECTION_MAX_RETRIES = 3
DB_CONNECTION_RETRY_DELAY = 1.0  # 秒
DB_INIT_CHECK_CACHE_SIZE = 100  # 表存在性检查缓存大小

# ============================================================================
# 日志配置 (Logging)
# ============================================================================
DEFAULT_LOG_MAX_FILE_SIZE = 10 * 1024 * 1024  # 10MB
DEFAULT_LOG_BACKUP_COUNT = 5
ASYNC_LOG_QUEUE_SIZE = 1000
LOG_COMPRESS_AFTER_DAYS = 7
LOG_CLEANUP_KEEP_DAYS = 30

# ============================================================================
# 同步配置 (Sync)
# ============================================================================
SYNC_CHECK_ON_STARTUP = True
SYNC_AUTO_ON_EXIT = True
SYNC_BACKGROUND_ENABLED = True

# ============================================================================
# 薄弱词筛选配置 (Weak Words Filtering)
# ============================================================================
# 评分权重
SCORE_WEIGHT_FAMILIARITY = 40  # 熟悉度权重（满分40）
SCORE_WEIGHT_REVIEW_COUNT = 20  # 复习次数权重（满分20）
SCORE_WEIGHT_TIME_DECAY = 10  # 时间衰减权重（满分10）
SCORE_WEIGHT_ITERATION = 10  # 迭代级别权重（满分10）

# 阈值配置
DEFAULT_WEAK_SCORE_THRESHOLD = 50.0  # 默认薄弱词分数阈值
DYNAMIC_THRESHOLD_ADJUSTMENT = 0.5  # 动态阈值调整幅度
WEAK_WORDS_DEFAULT_LIMIT = 100  # 默认返回的薄弱词数量

# 熟悉度阈值
FAMILIARITY_THRESHOLD_WEAK = 3.0  # 低于此值视为薄弱
FAMILIARITY_THRESHOLD_MODERATE = 5.0  # 5-7 为中等
FAMILIARITY_THRESHOLD_STRONG = 7.0  # 高于此值视为熟练

# ============================================================================
# AI 模型配置 (AI Models)
# ============================================================================
GEMINI_DEFAULT_MODEL = "gemini-2.0-flash"
MIMO_DEFAULT_MODEL = "mimo-v2-flash"
MIMO_DEFAULT_BASE_URL = "https://api.xiaomimimo.com/v1"

# AI 请求超时
AI_REQUEST_TIMEOUT_SECONDS = 60
AI_REQUEST_MAX_TOKENS = 4096

# ============================================================================
# 墨墨 API 限制 (Maimemo API Limits)
# ============================================================================
MAIMEMO_RATE_LIMIT_10S = 20  # 10秒内最多20次请求
MAIMEMO_RATE_LIMIT_60S = 40  # 60秒内最多40次请求
MAIMEMO_RATE_LIMIT_5H = 2000  # 5小时内最多2000次请求

# ============================================================================
# 文件路径常量 (File Paths)
# ============================================================================
DB_INIT_MARKER_DIR = "db_init_markers"
PROMPT_ARCHIVE_DIR = "prompts"

# ============================================================================
# 时间配置 (Time)
# ============================================================================
TIME_DECAY_MAX_DAYS = 30  # 时间衰减最大天数
DEFAULT_FUTURE_PREVIEW_DAYS = 7  # 默认预习天数
MAX_FUTURE_PREVIEW_DAYS = 14  # 最大预习天数

# ============================================================================
# 缓存配置 (Caching)
# ============================================================================
CACHE_TTL_SECONDS = 3600  # 缓存过期时间（1小时）
CACHE_MAX_SIZE = 1000  # 最大缓存条目数

# ============================================================================
# 用户会话配置 (User Session)
# ============================================================================
SESSION_TIMEOUT_MINUTES = 60
SESSION_CLEANUP_DAYS = 90  # 清理90天前的会话记录

# ============================================================================
# 性能监控配置 (Performance Monitoring)
# ============================================================================
PERFORMANCE_THRESHOLD_MS = 1000  # 性能警告阈值（毫秒）
SLOW_QUERY_THRESHOLD_MS = 500  # 慢查询阈值
