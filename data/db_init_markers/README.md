# DB Init Markers

Initialization marker flags are runtime artifacts used by local setup routines.

Policy:
- Marker files (`*.flag`) are not versioned.
- This placeholder keeps the directory structure.
